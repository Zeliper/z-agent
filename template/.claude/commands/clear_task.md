# /clear_task Command

z-agent 데이터(Task, Plan, Answer, Lesson)를 정리하는 명령어입니다.
자연어로 정리 대상을 지정할 수 있습니다.

## 사용 예시

```
/clear_task 완료된 task를 정리해줘
/clear_task 완료된 plan을 정리해줘
/clear_task PLAN-001을 삭제해줘
/clear_task task-003을 삭제해줘
/clear_task 모든 완료된 항목을 정리해줘
```

## 허용된 도구

- ✅ z_cleanup_preview (정리 대상 미리보기)
- ✅ z_get_tasks_by_status (상태별 Task 조회)
- ✅ z_get_plans_by_status (상태별 Plan 조회)
- ✅ z_delete_task (개별 Task 삭제)
- ✅ z_delete_plan (개별 Plan 삭제)
- ✅ z_delete_answer (개별 Answer 삭제)
- ✅ z_delete_lesson (개별 Lesson 삭제)
- ✅ z_delete_completed_tasks (완료된 Task 일괄 삭제)
- ✅ z_list_tasks, z_list_plans, z_list_answers, z_list_lessons (목록 조회)

## 실행 흐름

### 1. 사용자 요청 분석

사용자 입력에서 정리 대상을 파악합니다:
- "완료된 task" → completed tasks
- "완료된 plan" → completed plans
- "PLAN-XXX" → 특정 Plan
- "task-XXX" → 특정 Task
- "모든 완료된" → all completed

### 2. 미리보기 제공

**항상 삭제 전에 미리보기를 먼저 보여주세요!**

```
z_cleanup_preview(target: "completed_tasks")
→ 삭제될 항목 목록 표시
```

### 3. 사용자 확인 요청

⚠️ **중요: 삭제 전 반드시 사용자에게 확인을 받으세요!**

특히 다음 경우 주의:
- Plan에 미완료 Task가 연결된 경우
- 다수의 항목이 삭제되는 경우

```markdown
## 정리 대상 확인

### 삭제될 Tasks (3개)
- task-001: 기능 A 구현
- task-002: 버그 수정
- task-003: 테스트 작성

정말 삭제하시겠습니까? (예/아니오)
```

### 4. 삭제 실행

사용자 확인 후 삭제를 진행합니다.

## 시나리오별 처리

### A. 완료된 Task 정리

```
사용자: /clear_task 완료된 task 정리해줘

1. z_cleanup_preview(target: "completed_tasks")
   → 완료된 Task 목록 표시

2. 사용자에게 확인 요청
   "위 N개의 Task를 삭제하시겠습니까?"

3. 확인 후:
   z_delete_completed_tasks()
   → 모든 완료된 Task 삭제

4. 결과 보고
```

### B. 완료된 Plan 정리

```
사용자: /clear_task 완료된 plan 정리해줘

1. z_get_plans_by_status(status: "completed")
   → 완료된 Plan 목록 (연결된 Task 정보 포함)

2. 미완료 Task가 있는 경우 경고:
   "⚠️ PLAN-001에 미완료 Task(task-002)가 있습니다.
    - Plan만 삭제
    - Plan과 연결된 Task 모두 삭제
    - 취소"

3. 선택에 따라:
   z_delete_plan(planId: "PLAN-001", deleteLinkedTasks: true/false)
```

### C. 특정 항목 삭제

```
사용자: /clear_task PLAN-001 삭제해줘

1. z_get_plans_by_status(status: "all")
   → PLAN-001 정보 확인

2. 연결된 Task 확인 후 사용자에게 옵션 제시:
   "PLAN-001에 task-001, task-002가 연결되어 있습니다.
    - Plan만 삭제 (연결된 Task는 유지)
    - Plan과 Task 모두 삭제"

3. 선택에 따라 삭제 실행
```

### D. 개별 Task 삭제

```
사용자: /clear_task task-003 삭제해줘

1. z_get_tasks_by_status(status: "all")
   → task-003 존재 확인

2. 확인 요청:
   "task-003: 'XXX 구현'을 삭제하시겠습니까?"

3. 확인 후:
   z_delete_task(taskId: "task-003")
```

## 주의사항

### ⚠️ 필수 확인 사항

1. **미리보기 필수**: 삭제 전 항상 대상 목록을 보여줍니다
2. **사용자 확인 필수**: 삭제 작업은 되돌릴 수 없으므로 반드시 확인
3. **미완료 Task 경고**: Plan에 미완료 Task가 있으면 반드시 알림
4. **연결 관계 안내**: 연결된 항목이 있으면 처리 방법 선택지 제공

### 삭제 시 함께 삭제되는 것

| 삭제 대상 | 함께 삭제 |
|----------|----------|
| Task | Task 파일 + TODO 결과 디렉토리 |
| Plan | Plan 파일 (연결된 Task는 선택) |
| Answer | Answer 파일만 |
| Lesson | Lesson 파일만 |

## 출력 예시

```markdown
## 정리 완료

### 삭제된 항목
- ✅ task-001: 기능 A 구현
- ✅ task-002: 버그 수정
- ✅ task-003: 테스트 작성

### 요약
- 삭제된 Tasks: 3개
- 삭제된 파일: 12개

---
💡 정리된 항목은 복구할 수 없습니다.
```
